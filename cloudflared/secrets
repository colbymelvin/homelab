# <path relative to service dir>   [env_var]
# If env_var is set, the file is written as `<env_var>=<value>` (env-file format).
# Otherwise the raw value is written.
secrets/cloudflared_tunnel_token   TUNNEL_TOKEN
